@model IEnumerable<Testimonial>

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-wrapper">
    <div class="content">
        <!-- For Components documentaion -->
        <div class="row card-header justify-content-between">

            <h2 class="col">Pending Testimonial</h2>
            <a asp-action="Testimonials" class="mb-1 btn btn-primary col-2">Back</a>
        </div>

        <div class="row">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">@Html.DisplayNameFor(model => model.Cust.Username)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.Testimonialstatus.Statusname)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.Posteddate)</th>
                       <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                       
                            <tr class="table-primary">

                            <td scope="row"><a asp-action="CustomerDetails" asp-route-userid="@item.Custid">@item.Cust.Username</a></td>

                            <td>@item.Testimonialstatus.Statusname</td>
                                <td>@item.Posteddate.DateTime</td>
                            <td><a asp-controller="Testimonials" asp-action="Details" class="btn btn-sm btn-outline-info mr-4" asp-route-id="@item.Testumonialid">Details </a><a class="btn btn-sm btn-outline-success mr-4" asp-action="AcceptTestimonials" asp-route-id="@item.Testumonialid">Accept</a> <a class="btn btn-sm btn-outline-danger" asp-action="RejectTestimonials" asp-route-id="@item.Testumonialid">Reject</a><a asp-controller="Testimonials" asp-action="Delete" class="btn btn-sm btn-outline-danger ml-lg-4 ml-md-3 ml-sm-2" asp-route-id="@item.Testumonialid">Delete </a></td>
                            </tr>
                     
                        
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>